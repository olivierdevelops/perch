apiVersion: apps/v1
kind: Deployment
metadata:
  name: api
spec:
  template:
    spec:
      containers:
        - name: api
          image: docker.io/some-random-user/api:latest
          securityContext:
            privileged: true
